services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: wapu_bot
      POSTGRES_USER: wapu_bot
      POSTGRES_PASSWORD: wapu_bot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
